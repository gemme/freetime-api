{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongo": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "",
    "database": "${DB_DATABASE}",
    "password": "${DB_PASS}",
    "name": "mongo",
    "user": "${DB_USER}",
    "connector": "mongodb"
  },
  "media": {
    "name": "media",
    "connector": "loopback-component-storage",
    "provider": "amazon",
    "key": "${MEDIA_KEY}",
    "keyId": "${MEDIA_KEY_ID}"
  }
}
