{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongo": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "",
    "database": "freetime",
    "password": "${DB_PASS}",
    "name": "mongo",
    "user": "${DB_USER}",
    "connector": "mongodb"
  }
}
